const mongoose = require('mongoose');

const maliciousArraySchema = new mongoose.Schema({
    Malicious: mongoose.SchemaTypes.Array
});

module.exports = mongoose.model('MaliciousArray', maliciousArraySchema);